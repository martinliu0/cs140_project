{% extends "base.html" %}
{% load staticfiles %}

<!DOCTYPE html>
<html>
    {% block main %}
        <p><h2><span class="recipename">{{ recipe.name }}</span></h2></p>
        <br>
        {% if recipe.image.url.width > recipe.image.url.height %}
            <img src="{{ recipe.image.url }}" width=100>
        {% else %}
            <img src="{{ recipe.image.url }}" height=100>
        {% endif %}
        <br><br>
        <p class="description">{{ recipe.description }}
        <br>
        <div class="review"><p>Reviews</div>
            {% if recipe.review_set %}
                {% for review in recipe.review_set.all %}
                    <li class="new-review">{{ review }}</li>
                {% endfor %}
            {% endif %}
            <p><a href="{% url 'review' recipe.id %}">Write a review</a>
    {% endblock main %}
</html>
