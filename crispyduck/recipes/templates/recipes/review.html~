{% extends "base.html" %}

{% block main %}

<section class="reviewtemplate">
    <center><h2>Write a review for {{recipe.name}}!</h2></center>
        {% if recipe.image.url.width > recipe.image.url.height %}
            <center><img src="{{recipe.image.url}}" width =300></center>
        {% else %}
            <center><img src="{{recipe.image.url}}" height=300></center>
        {% endif %}
        <br>
        <form action="{% url 'review' id=recipe.id %}" method="post">
            {% csrf_token %}
            {{ form.as_table }}
            <input type="submit" value="Add review">
        </form> 
                    
<br>
<p>Back to recipe <a href="{% url 'details' id=recipe.id %}">details</a>
<p>Back to <a href="{% url 'index' %}">search results</a>
{% endblock main %}
</section>
