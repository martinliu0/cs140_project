{% extends "base.html" %}
{% block main %}

<section class="index">
<section class="navigation">
      <table>
        <tr>
          <th><button type="button" class="btn btn-appetizer btn-sm"><a href ="{% url 'index' %}?sort=AT">Appetizers</a></button></th>
          <th><button type="button" class="btn btn-main btn-sm"><a href ="{% url 'index' %}?sort=MN">Mains</a></button></th>
          <th><button type="button" class="btn btn-dessert btn-sm"><a href ="{% url 'index' %}?sort=DS">Desserts</a></button></th>
          <th><button type="button" class="btn btn-restaurants btn-sm"><a href="{% url 'restaurants' %}">Don't feel like cooking?</a></button></th>
        </tr>
        </table>
</section>
<div class="recipe-list">
	{% for recipe in recipes %}
		<div class="recipe">
			<div class="image">
				<!-- <img class="avatar" src="{{ recipe.image.url }}"> -->
				<div class="avatar" style="background-image: url('{{ recipe.image.url }}')"></div>
			</div>
			<div class="recipe-block">
				<a href="{% url 'details' recipe.id %}"><h1>{{ recipe.name }}</h1></a>
				<p class="recipe-description"> {{ recipe.description|truncatewords:100 }}</p>
					<div class="other-stuff">
					    <ul class="meal-time">
					       <li class="meal">
							{% if recipe.meal == 'MN'%}
								<div class="btn btn-primary-outline">Main</div>
							{% elif recipe.meal == 'DS' %}
								<div class="btn btn-success-outline">Dessert</div>
							{% else %}
								<div class="btn btn-info-outline" nohover>Appetizer</div>
							{% endif %}
							</li>
							<li class="time">{{ recipe.get_time_str }}</li>
					    </ul>
						
						
						<ul class="recipe-actions">
							<li class="save-recipe">Save</li>
							<li class="review"><a href="{% url 'review' recipe.id %}">Review</a></li>
						</ul>
					</div>
				</div>
			</div>
    {% endfor %}
</div>
<center><p>Want to search again? Back to <a href="{% url 'main' %}?main=true">main</a></center>
{% endblock main %} 
</section>
