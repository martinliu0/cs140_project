{% extends "base.html" %}
{% block main %}
<section id="filters">
  <form action="{% url 'index' %}" method="get">
    {{ search_form.as_p }}
    <input type="submit" value="Filter products">
  </form>
  <p><a href="{% url 'index' %}?reset=true">Reset filters</a>
</section>

<div class="recipe-list">
	{% for recipe in recipes %}
		<div class="recipe">
			<div class="image">
				<!-- <img class="avatar" src="{{ recipe.image.url }}"> -->
				<div class="avatar" style="background-image: url('{{ recipe.image.url }}')"></div>
			</div>
			<div class="recipe-block">
				<a href="{% url 'details' recipe.id %}"><h1>{{ recipe.name }}</h1></a>
				<p class="recipe-description"> {{ recipe.description|truncatewords:100 }}</p>
					<div class="other-stuff">
						<div class="meal">
							{% if recipe.meal == 'MN'%}
								<button type="button" class="btn btn-primary-outline">Main</button>
							{% elif recipe.meal == 'DS' %}
								<button type="button" class="btn btn-success-outline">Dessert</button>
							{% else %}
								<span type="button" class="btn btn-info-outline" nohover>Appetizer</span>
							{% endif %}
						</div>
						<div class="time">{{ recipe.get_time_str }}</div>
						<ul class="recipe-actions">
							<li class="save-recipe">Save</li>
							<li class="review"><a href="{% url 'review' recipe.id %}">Review</a></li>
						</ul>
					</div>
				</div>
			</div>
    {% endfor %}

</div>
{% endblock main %} 
