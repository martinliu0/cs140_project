{% extends "base.html" %}
{% block main %}


<section id="navigation">
      <table>
        <tr>
          <th><a href ="{% url 'index' %}?sort=appetizer">Appetizers</a></th>
          <th><a href ="{% url 'index' %}?sort=main">Mains</a></th>
          <th><a href ="{% url 'index' %}?sort=dessert">Desserts</a></th>
          <th>Don't feel like cooking?</th>
        </tr>
        </table>
</section>
<div class="recipe-list">
	{% for recipe in recipes %}
		<div class="recipe">
			<div class="image">
				<!-- <img class="avatar" src="{{ recipe.image.url }}"> -->
				<div class="avatar" style="background-image: url('{{ recipe.image.url }}')"></div>
			</div>
			<div class="recipe-block">
				<a href="{% url 'details' recipe.id %}"><h1>{{ recipe.name }}</h1></a>
				<p class="recipe-description"> {{ recipe.description|truncatewords:100 }}</p>
					<div class="other-stuff">
					    <ul class="meal-time">
					       <li class="meal">
							{% if recipe.meal == 'MN'%}
								<div type="button" class="btn btn-primary-outline">Main</div>
							{% elif recipe.meal == 'DS' %}
								<div type="button" class="btn btn-success-outline">Dessert</div>
							{% else %}
								<div type="button" class="btn btn-info-outline" nohover>Appetizer</div>
							{% endif %}
							</li>
							<li class="time">{{ recipe.get_time_str }}</li>
					    </ul>
						
						
						<ul class="recipe-actions">
							<li class="save-recipe">Save</li>
							<li class="review"><a href="{% url 'review' recipe.id %}">Review</a></li>
						</ul>
					</div>
				</div>
			</div>
    {% endfor %}
</div>
<p>Want to search again? Back to <a href="{% url 'main' %}">main</a>
{% endblock main %} 
